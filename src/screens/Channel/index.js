import React, { Component } from 'react'
import CustomHeader from '../../components/Header'

const Channel = () => {
  return <CustomHeader showSubtitle={true} subtitle="CANAL PRATICANDO" />
}

export default Channel
